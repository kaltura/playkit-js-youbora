## CI - Player And Plugin Tests
name: Player And Plugin Tests
run-name: Player And Plugin Tests

on:
  pull_request:
    branches:
      - devops-pr

jobs:
  running-tests:
    uses: kaltura/ovp-pipelines/.github/workflows/tests.yaml@master
    with:
      yarn-run-to-execute: 'eslint flow'


